import fs from 'fs'; import path from 'path'; import {fileURLToPath} from 'url'; import {pool} from '../src/db.js'; const dir=path.dirname(fileURLToPath(import.meta.url));await pool.query(fs.readFileSync(path.join(dir,'001_initial.sql'),'utf8'));console.log('Migration complete');await pool.end();
