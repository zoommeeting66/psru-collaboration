import bcrypt from 'bcryptjs'; import {pool} from '../src/db.js'; const pass=await bcrypt.hash('PsrU@123',12);const admin=(await pool.query("INSERT INTO roles(name,description) VALUES('admin','ผู้ดูแลระบบ'),('officer','เจ้าหน้าที่'),('executive','ผู้บริหาร') ON CONFLICT(name) DO UPDATE SET name=EXCLUDED.name RETURNING id,name")).rows;const dept=(await pool.query("INSERT INTO departments(name,code) VALUES('กองพัฒนานักศึกษา','DSD'),('คณะวิทยาศาสตร์และเทคโนโลยี','SCI') ON CONFLICT(code) DO UPDATE SET name=EXCLUDED.name RETURNING id,code")).rows;await pool.query("INSERT INTO users(name,email,password_hash,role_id,department_id) VALUES('ผู้ดูแลระบบ','admin@psru.ac.th',$1,$2,$3) ON CONFLICT(email) DO NOTHING",[pass,admin.find(x=>x.name==='admin').id,dept[0].id]);const p=(await pool.query("INSERT INTO partner_organizations(name,type,country) VALUES('มหาวิทยาลัยเชียงใหม่','มหาวิทยาลัย','ไทย'),('บริษัท เทคโนโลยีไทย จำกัด','ภาคเอกชน','ไทย') ON CONFLICT DO NOTHING RETURNING id")).rows;const u=(await pool.query("SELECT id FROM users WHERE email='admin@psru.ac.th'")).rows[0];if(p[0])await pool.query("INSERT INTO collaborations(code,title_th,type,department_id,start_date,end_date,status,objective,created_by) VALUES('MOU-2569-001','ความร่วมมือด้านการวิจัยและนวัตกรรม','MOU',$1,'2026-01-01','2027-12-31','ACTIVE','ส่งเสริมงานวิจัยร่วมกัน',$2) ON CONFLICT(code) DO NOTHING",[dept[0].id,u.id]);console.log('Seed complete: admin@psru.ac.th / PsrU@123');await pool.end();
