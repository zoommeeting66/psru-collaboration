import 'express-async-errors'; import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import morgan from 'morgan'; import dotenv from 'dotenv'; import auth from './routes/auth.js'; import master from './routes/master.js'; import collaborations from './routes/collaborations.js'; import dashboard from './routes/dashboard.js'; import files from './routes/files.js'; import reports from './routes/reports.js'; import users from './routes/users.js'; import {notFound,errorHandler} from './middleware/errors.js'; dotenv.config();
const app=express();app.use(helmet({crossOriginResourcePolicy:false}));app.use(cors({origin:process.env.CLIENT_URL,credentials:true}));app.use(express.json({limit:'1mb'}));app.use(morgan('dev'));app.use('/uploads',express.static('uploads'));app.get('/api/health',(req,res)=>res.json({status:'ok'}));app.use('/api/auth',auth);app.use('/api/users',users);app.use('/api',master);app.use('/api/collaborations',collaborations);app.use('/api/dashboard',dashboard);app.use('/api/documents',files);app.use('/api/reports',reports);app.use(notFound);app.use(errorHandler);app.listen(process.env.PORT||4000,()=>console.log('API ready'));
