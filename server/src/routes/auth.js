import { Router } from 'express'; import bcrypt from 'bcryptjs'; import jwt from 'jsonwebtoken'; import { body,validationResult } from 'express-validator'; import { query } from '../db.js'; import { authenticate } from '../middleware/auth.js';
const r=Router();
r.post('/login',[body('email').isEmail(),body('password').isLength({min:6})],async(req,res)=>{const e=validationResult(req);if(!e.isEmpty())return res.status(422).json({message:'ข้อมูลเข้าสู่ระบบไม่ถูกต้อง'});const {rows}=await query('SELECT u.*,r.name role FROM users u JOIN roles r ON r.id=u.role_id WHERE u.email=$1',[req.body.email]);const u=rows[0];if(!u||!u.is_active||!await bcrypt.compare(req.body.password,u.password_hash))return res.status(401).json({message:'อีเมลหรือรหัสผ่านไม่ถูกต้อง'});const token=jwt.sign({id:u.id},process.env.JWT_SECRET,{expiresIn:'8h'});res.json({token,user:{id:u.id,name:u.name,email:u.email,role:u.role}});});
r.get('/me',authenticate,(req,res)=>res.json(req.user)); export default r;
