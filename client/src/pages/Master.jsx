import{useEffect,useState}from'react';import{api}from'../api';import{Plus,Trash2}from'lucide-react';export default function Master({kind,title}){const[items,setItems]=useState([]),[name,setName]=useState('');const load=()=>api.get('/'+kind).then(x=>setItems(x.data));useEffect(load,[]);const add=async e=>{e.preventDefault();if(!name.trim())return;await api.post('/'+kind,{name});setName('');load()};const del=async id=>{if(confirm('ยืนยันการลบข้อมูล?')){await api.delete(`/${kind}/${id}`);load()}};return <><div className="mb-5"><h2 className="text-xl font-bold text-psru-blue">{title}</h2><p className="text-sm text-slate-500">เพิ่มและจัดการข้อมูลอ้างอิงของระบบ</p></div><div className="grid gap-6 lg:grid-cols-3"><form className="card h-fit p-5" onSubmit={add}><h3 className="mb-4 font-semibold">เพิ่มรายการใหม่</h3><input className="input mb-3" placeholder="ชื่อรายการ" value={name} onChange={e=>setName(e.target.value)}/><button className="btn flex items-center gap-2"><Plus size={16}/>บันทึก</button></form><div className="card overflow-hidden lg:col-span-2"><table className="w-full text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="p-4 text-left">ชื่อ</th><th className="p-4 text-left">ประเภท / ประเทศ</th><th className="p-4"></th></tr></thead><tbody>{items.map(x=><tr className="border-t" key={x.id}><td className="p-4 font-medium">{x.name}</td><td className="p-4 text-slate-500">{x.type||x.code||'-'} {x.country&&`· ${x.country}`}</td><td className="p-4 text-right"><button className="text-rose-500" onClick={()=>del(x.id)}><Trash2 size={17}/></button></td></tr>)}</tbody></table></div></div></>}
