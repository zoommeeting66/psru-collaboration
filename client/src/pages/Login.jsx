import{useState}from'react';import{useNavigate}from'react-router-dom';import{LogIn,ShieldCheck}from'lucide-react';import{api}from'../api';const demo={email:'admin@psru.ac.th',password:'PsrU@123'};export default function Login(){const[email,setEmail]=useState(demo.email),[password,setPassword]=useState(demo.password),[err,setErr]=useState(''),[loading,setLoading]=useState(false),n=useNavigate();const submit=async e=>{e.preventDefault();setErr('');setLoading(true);try{const{data}=await api.post('/auth/login',{email,password});localStorage.setItem('token',data.token);localStorage.setItem('user',JSON.stringify(data.user));n('/')}catch(x){setErr(x.response?.data?.message||'เชื่อมต่อระบบไม่ได้ กรุณาเปิดระบบด้วย start-app.cmd ก่อน')}finally{setLoading(false)}};return <main className="grid min-h-screen place-items-center bg-gradient-to-br from-psru-blue to-blue-950 p-5"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-8 shadow-2xl"><div className="mb-6 text-center"><div className="mx-auto mb-3 grid h-14 w-14 place-items-center rounded-full bg-psru-gold text-xl font-bold text-white">PS</div><h1 className="text-lg font-bold text-psru-blue">ระบบฐานข้อมูลบันทึกความร่วมมือ</h1><p className="text-sm text-slate-500">มหาวิทยาลัยราชภัฏพิบูลสงคราม</p></div><div className="mb-5 rounded-xl border border-blue-100 bg-blue-50 p-3 text-sm text-blue-900"><p className="flex items-center gap-1.5 font-semibold"><ShieldCheck size={16}/>บัญชีสำหรับเริ่มใช้งาน</p><p className="mt-1 text-xs">ข้อมูลถูกกรอกไว้แล้ว กดปุ่มด้านล่างเพื่อเข้าสู่ระบบได้ทันที</p></div>{err&&<p className="mb-3 rounded-lg bg-rose-50 p-3 text-sm text-rose-600">{err}</p>}<label className="text-sm">อีเมล</label><input className="input mt-1 mb-4" type="email" value={email} onChange={e=>setEmail(e.target.value)}/><label className="text-sm">รหัสผ่าน</label><input className="input mt-1 mb-5" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn flex w-full items-center justify-center gap-2" disabled={loading}>{loading?'กำลังเข้าสู่ระบบ...':<><LogIn size={17}/>เข้าสู่ระบบ</>}</button><p className="mt-4 text-center text-xs text-slate-400">เข้าสู่ระบบครั้งแรกแล้วควรเปลี่ยนรหัสผ่าน</p></form></main>}
