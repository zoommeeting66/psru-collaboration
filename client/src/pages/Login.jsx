import{useState}from'react';import{useNavigate}from'react-router-dom';import{api}from'../api';export default function Login(){const[email,setEmail]=useState('admin@psru.ac.th'),[password,setPassword]=useState('PsrU@123'),[err,setErr]=useState(''),n=useNavigate();const submit=async e=>{e.preventDefault();try{const{data}=await api.post('/auth/login',{email,password});localStorage.setItem('token',data.token);localStorage.setItem('user',JSON.stringify(data.user));n('/')}catch(x){setErr(x.response?.data?.message||'ไม่สามารถเชื่อมต่อระบบได้')}};return <main className="grid min-h-screen place-items-center bg-gradient-to-br from-psru-blue to-blue-950 p-5"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-8 shadow-2xl"><div className="mb-7 text-center"><div className="mx-auto mb-3 grid h-14 w-14 place-items-center rounded-full bg-psru-gold text-xl font-bold text-white">PS</div><h1 className="text-lg font-bold text-psru-blue">ระบบฐานข้อมูลบันทึกความร่วมมือ</h1><p className="text-sm text-slate-500">มหาวิทยาลัยราชภัฏพิบูลสงคราม</p></div>{err&&<p className="mb-3 rounded bg-rose-50 p-2 text-sm text-rose-600">{err}</p>}<label className="text-sm">อีเมล</label><input className="input mt-1 mb-4" value={email} onChange={e=>setEmail(e.target.value)}/><label className="text-sm">รหัสผ่าน</label><input className="input mt-1 mb-6" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn w-full">เข้าสู่ระบบ</button><p className="mt-4 text-center text-xs text-slate-400">บัญชีเริ่มต้น: admin@psru.ac.th</p></form></main>}
