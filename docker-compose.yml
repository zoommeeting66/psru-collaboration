services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: psru_collaboration
      POSTGRES_USER: psru
      POSTGRES_PASSWORD: psru_password
    ports: ["5433:5432"]
    volumes: ["postgres_data:/var/lib/postgresql/data"]
volumes:
  postgres_data:
